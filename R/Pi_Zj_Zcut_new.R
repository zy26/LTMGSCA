################################################################
Pi_Zj_Zcut_new <- function(q, mean, sd, wl0) {
  a <- wl0 * pnorm(q, mean, sd)  ################################################################
  if(sum(a) == 0) return(wl0)
  return(a / sum(a))
}
